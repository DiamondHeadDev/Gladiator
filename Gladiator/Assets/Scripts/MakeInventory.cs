using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MakeInventory : MonoBehaviour
{
    /* What I want to do here is load a dictionary called Inventory.
     * Then, it should generate with instantiate method 
     * the prefabs in respect to the dictionary for me
     */

    // Start is called before the first frame update
    void Start()
    {
//	Public void Instantiate(Item.Helmet); // This is where the prefabs should be made
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
